%%%===================================================================
%%% Generated by generate_record.rake 
%%%===================================================================

-include("include/config_data_records.hrl").
-record(alliances, {
        uuid,
        president_id,
        name,
        level,
        desc,
        founds,
        officer_ids,
        elder_ids,
        elite_ids,
        member_ids,
        created_at,
        store_slot,
        accept_app,
        president_name}).

-record(alliance_activity_unlocks, {
        uuid,
        alliance_id,
        conf_id,
        unlocked}).

-record(alliance_applications, {
        uuid,
        owner_id,
        alliance_id,
        user_id,
        created_at}).

-record(alliance_auctions, {
        uuid,
        alliance_id,
        item_id,
        price,
        user_id,
        finish_at}).

-record(alliance_bosses, {
        uuid,
        alliance_id,
        conf_id,
        hp,
        first_hitter,
        hitters,
        level,
        created_at,
        start_at,
        stop_at}).

-record(alliance_helps, {
        uuid,
        alliance_id,
        user_id,
        helped_members}).

-record(alliance_researches, {
        uuid,
        alliance_id,
        name,
        level}).

-record(alliance_shop_items, {
        uuid,
        alliance_id,
        category,
        name,
        conf_id,
        amount}).

-record(contest_records, {
        uuid,
        user_id,
        contest_id,
        arena_level,
        group,
        rank,
        rewarded}).

-record(copies, {
        uuid,
        user_id,
        area_id,
        copy_id,
        star,
        battle_process,
        is_rewarded,
        conf_id}).

-record(equips, {
        uuid,
        user_id,
        level,
        conf_id,
        evolves,
        equiped,
        exp}).

-record(formations, {
        uuid,
        user_id,
        matrix,
        formation_type}).

-record(gem_slots, {
        uuid,
        hero_id,
        user_id,
        slot_1,
        slot_2,
        slot_3,
        slot_4,
        slot_5,
        slot_6,
        slot_7,
        slot_8,
        slot_9,
        slot_10}).

-record(gemstones, {
        uuid,
        user_id,
        conf_id,
        amount}).

-record(grids, {
}).

-record(heros, {
        uuid,
        user_id,
        level,
        config_id,
        created_at,
        updated_at,
        hp,
        hp_updated_at,
        max_hp,
        recover,
        exp,
        weapon_slot,
        armor_slot,
        accessory_slot,
        alliance_slot,
        is_main,
        star,
        fragments,
        skill_slot}).

-record(items, {
        uuid,
        user_id,
        type,
        conf_id,
        amount}).

-record(letters, {
        uuid,
        number,
        category,
        subject,
        content,
        sender_id,
        sender_name,
        receiver_id,
        sent_at,
        is_read,
        is_claimed,
        attachment}).

-record(messages, {
        uuid,
        receiver_id,
        sender_id,
        sender,
        subject,
        content,
        attachment,
        category,
        is_read,
        is_claimed,
        number,
        created_at}).

-record(player_summaries, {
        uuid,
        finished_normal_waypoints,
        finished_elite_waypoints,
        finished_talks}).

-record(quests, {
        uuid,
        user_id,
        type,
        conf_id,
        finished,
        rewarded,
        progress,
        status}).

-record(quest_summaries, {
        uuid,
        user_id,
        finished,
        opened,
        last_main_quest}).

-record(server_events, {
        at_time,
        server_id,
        title,
        content,
        layout}).

-record(server_notices, {
        at_time,
        server_id,
        title,
        content}).

-record(towns, {
        uuid,
        user_id,
        name,
        x,
        y,
        created_at,
        updated_at}).

-record(users, {
        uuid,
        udid,
        name,
        gem,
        paid,
        level,
        soul,
        money,
        bag,
        created_at,
        updated_at,
        ap,
        ap_updated_at,
        area_id,
        position,
        prestige_level,
        prestige_remain,
        prestige_used,
        battle_win_amount,
        battle_amount,
        battle_score,
        arena_level,
        arena_levels,
        bp,
        alliance_id,
        alliance_name,
        friend_ids,
        block_ids,
        conf_id,
        alliance_position,
        dkp,
        locale,
        device_token,
        formulas,
        skill_points,
        team_leader,
        server_id,
        active_at,
        is_online,
        abilities,
        free_loto_at,
        daily_awards,
        sublv_awards,
        duration_awards,
        exp,
        current_quest_id,
        current_waypoint_id,
        battle_items}).

-record(user_letters, {
        uuid,
        user_id,
        category,
        oldest_number,
        newest_number}).

-record(wanteds, {
        uuid,
        alliance_id,
        publisher,
        publisher_id,
        wanted_type,
        wanted_id,
        status,
        rewarded,
        history,
        helper_id,
        help_at,
        created_at}).

