%%%===================================================================
%%% Generated by generate_record.rake 2014-03-11 11:41:18 +0800
%%%===================================================================

-record(config_items, {
        id,
        title,
        ver,
        obj,
        number,
        no,
        url,
        pc,
        ken}).

-record(config_players, {
        id,
        config_id,
        name,
        name_desc,
        profession,
        skill_id,
        attack_type,
        rare,
        health,
        attack,
        defense,
        recover,
        skill_power,
        crit,
        crit_damage,
        accurate,
        dodge,
        parry,
        block}).

-record(config_player_grows, {
        id,
        config_id,
        name,
        name_desc,
        health,
        attack,
        defense,
        recover,
        skill_power,
        crit,
        crit_damage,
        accurate,
        dodge,
        parry,
        block}).

-record(config_stores, {
        id,
        name,
        amount}).

-record(config_users, {
        id,
        level,
        exp,
        title,
        title_desc}).

-record(formations, {
        uuid,
        user_id,
        matrix}).

-record(heros, {
        uuid,
        user_id,
        level,
        config_id,
        created_at,
        updated_at}).

-record(towns, {
        uuid,
        user_id,
        name,
        x,
        y,
        created_at,
        updated_at}).

-record(users, {
        uuid,
        udid,
        name,
        gem,
        paid,
        created_at,
        updated_at}).

